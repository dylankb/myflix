%article.video_category
  %header
    - @categories.each do |category|
      .videos.row
        %h3= category.name
        - category.recent_videos.each do |video|
          .video.col-sm-2
            = link_to video do
              %img(src="#{video.thumbnail_image_url}")
